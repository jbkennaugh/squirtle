{
  "currentUser": "query getCurrentUser {currentUser {id name}}",
  "event": "query EventQuery($slug:String) {event(slug: $slug) {id name}}",
  "phaseId": "query PhaseQuery($slug:String) {event(slug: $slug) {phases {id}}}",
  "streamQueue": "query StreamQueueOnTournament($slug: String!) {tournament(slug: $slug) {id streamQueue {stream {streamSource streamName} sets {event {id} id fullRoundText state slots {entrant {id participants {gamerTag} seeds {seedNum}}}}}}}",
  "standings": "query EventStandings($eventId: ID!, $page: Int!, $perPage: Int!) {event(id: $eventId) {id name standings(query: {perPage: $perPage,page: $page}){nodes {placement entrant {id name}}}}}",
  "sets": "query EventSets($eventId: ID!, $page: Int!, $perPage: Int!) { event(id: $eventId) {sets(page: $page perPage: $perPage sortType: STANDARD) {pageInfo {total} nodes {id slots {entrant {name}}}}}}",
  "tournamentsByRole": "query TournamentsByRole($userId: ID!, $role: String!) {user(id: $userId) {tournaments(query: {filter: {tournamentView:$role upcoming: true}}) {nodes {id name slug events {id name slug} }}}}",
  "reportSet": "mutation reportSet($setId: ID!, $winnerId: ID, $gameData: [BracketSetGameDataInput]) {reportBracketSet(setId: $setId, winnerId: $winnerId, gameData: $gameData) {id state}}",
  "markSetInProgress": "mutation markSetInProgress($setId: ID!) {markSetInProgress(setId: $setId) {id}}",
  "resetSet": "mutation resetSet($setId: ID!) {resetSet(setId: $setId) {id}}"
}
